# The showcase without the two lines that separate the front page body from
# the generated lists: no llmsNote, no llms-full.txt. Nothing else changes.
[outputs]
  home = ["HTML", "RSS", "JSON", "llms", "md"]

[languages]
  [languages.en.params]
    llmsNote = ""
  [languages.fr.params]
    llmsNote = ""
  [languages.ja.params]
    llmsNote = ""
  [languages.ar.params]
    llmsNote = ""
