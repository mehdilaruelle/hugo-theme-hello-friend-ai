+++
noindex = true
+++

This page's front matter declares no title. Hugo leaves `.Title` empty for a
file you wrote, so the theme falls back to the file name and humanizes it — the
tab, the card and the structured data all end up naming the page the same way.
